# Core dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
websockets>=11.0.3

# Authentication & Security
pyjwt==2.8.0
bcrypt==4.1.2
python-multipart==0.0.6
google-auth>=2.23.0

# ML and data processing
numpy>=1.26
scikit-learn>=1.3.2
pandas>=2.1.3
joblib>=1.5.3

# Audio processing
librosa>=0.10.0
soundfile>=0.12.1
scipy>=1.11.4

# Speech Recognition & Whisper
openai-whisper

# Noise reduction
noisereduce==3.0.3

# Audio format conversion
pydub>=0.25.1

# Database (SQLite for dev, extendable to PostgreSQL)
sqlalchemy==2.0.23

# MongoDB
motor>=3.3.0  # Async MongoDB driver
pymongo>=4.5.0  # MongoDB driver

# Testing
requests==2.31.0
pytest==7.4.3

# NLP and Text Processing
spacy==3.7.2
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
nltk==3.8.1
# transformers 4.47.1: supports Llama 3.2 rope_scaling + has BertModel for sentence-transformers
# Must stay <5.0 because transformers 5.x requires PyTorch >=2.4 (we use 2.3.1)
transformers==4.47.1
torch==2.3.1
# NOTE: do NOT install torchvision — it conflicts with torch==2.3.1 (torchvision needs 2.11.0)
peft>=0.13.0  # 0.10.0 missing 'alora_invocation_tokens' — LoRA adapter fails to load
sentence-transformers==2.7.0  # Must be 2.7.x — 3.x+ conflicts with transformers 4.x
textblob==0.17.1

langdetect==1.0.9
gensim==4.3.2
accelerate>=0.34.0  # For model loading optimization
huggingface-hub>=0.36.0,<1.0.0
imbalanced-learn>=0.12.0  # Required by reminder confusion detection model


# Chronos Time-Series Forecasting (behavioral dementia risk analysis)
chronos-forecasting>=1.3.0

# Development
python-dotenv==1.0.0
email-validator>=2.0.0  # Required by pydantic EmailStr fields in caregiver_routes
dateparser>=1.1.0  # Required by NLP date parsing modules
